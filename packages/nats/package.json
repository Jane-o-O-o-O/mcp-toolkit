{
  "name": "@mcp-toolkit/nats",
  "version": "0.1.0",
  "type": "module",
  "description": "NATS MCP Server — pub/sub and JetStream message streaming",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "mcp-nats": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@mcp-toolkit/core": "workspace:*",
    "@mcp-toolkit/logger": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "nats": "^2.29.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}